{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Node.js 環境のため DOM ライブラリは不要
    "lib": ["ESNext"],
    // パスエイリアス設定
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@/types": ["types/shared-types.ts"]
    }
  }
}
