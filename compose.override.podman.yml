# Podman-specific overrides.
# Usage: cp compose.override.podman.yml compose.override.yml && podman-compose up -d
#   or:  podman-compose -f compose.yml -f compose.override.podman.yml up -d

services:
  cms-db:
    userns_mode: "keep-id:uid=999,gid=999"

  frontend:
    userns_mode: "keep-id"
    volumes:
      - .:/app:rw
      - node_modules:/app/node_modules:rw,U
      - frontend_node_modules:/app/apps/frontend/node_modules:rw,U
      - backend_node_modules:/app/apps/backend/node_modules:rw,U
      - eslint_config_node_modules:/app/packages/eslint-config/node_modules:rw,U

  backend:
    userns_mode: "keep-id"
    volumes:
      - .:/app:rw
      - node_modules:/app/node_modules:rw,U
      - frontend_node_modules:/app/apps/frontend/node_modules:rw,U
      - backend_node_modules:/app/apps/backend/node_modules:rw,U
      - eslint_config_node_modules:/app/packages/eslint-config/node_modules:rw,U

  elasticsearch:
    userns_mode: "keep-id:uid=1000,gid=1000"
